<Window x:Class="WordOverlayProofreader.Overlay.SuggestionWidget" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Grammar Suggestions" Height="180" Width="320" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">
  <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
    <Border.Effect>
      <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1" Color="Black" />
    </Border.Effect>

    <Grid Margin="16">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Header -->
      <Grid Grid.Row="0" Margin="0,0,0,12">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
             <TextBlock x:Name="ErrorTypeText" Text="SPELLING" FontWeight="Bold" FontSize="10" Foreground="#F44336" />
             <TextBlock x:Name="CountText" Text="1 / 5" FontSize="10" Foreground="#9E9E9E" Margin="8,0,0,0" VerticalAlignment="Center" />
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Button x:Name="PreviousButton" Content="‹" Click="PreviousButton_Click" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#757575" Padding="4,0" Cursor="Hand" Margin="0,0,4,0"/>
            <Button x:Name="NextButton" Content="›" Click="NextButton_Click" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="#757575" Padding="4,0" Cursor="Hand"/>
        </StackPanel>
      </Grid>

      <!-- Content -->
      <StackPanel Grid.Row="1" VerticalAlignment="Center">
        <TextBlock x:Name="OriginalText" Text="misteake" FontSize="14" Foreground="#BDBDBD" TextDecorations="Strikethrough" Margin="0,0,0,4" TextTrimming="CharacterEllipsis" />
        <TextBlock x:Name="SuggestionText" Text="mistake" FontSize="18" FontWeight="SemiBold" Foreground="#212121" TextWrapping="Wrap" />
      </StackPanel>

      <!-- Actions -->
      <Grid Grid.Row="2" Margin="0,12,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="10" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Button Grid.Column="0" x:Name="DismissButton" Content="Dismiss" Click="DismissButton_Click" Background="#F5F5F5" Foreground="#757575" BorderThickness="0" Padding="0,8" FontSize="12" FontWeight="Medium" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Button.Resources>
        </Button>
        
        <Button Grid.Column="2" x:Name="AcceptButton" Content="Accept" Click="AcceptButton_Click" Background="#2196F3" Foreground="White" BorderThickness="0" Padding="0,8" FontSize="12" FontWeight="Medium" Cursor="Hand">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Button.Resources>
        </Button>
      </Grid>
    </Grid>
  </Border>
</Window>