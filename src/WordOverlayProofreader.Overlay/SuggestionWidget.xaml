<Window x:Class="WordOverlayProofreader.Overlay.SuggestionWidget" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Suggestions" Height="160" Width="340" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">
  <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="10">
    <Border.Effect>
      <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.15" Color="#000000" />
    </Border.Effect>

    <Grid Margin="20,16,20,16">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Header with navigation -->
      <Grid Grid.Row="0" Margin="0,0,0,10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Ellipse Width="8" Height="8" x:Name="ErrorTypeDot" Fill="#15C39A" Margin="0,0,6,0" />
          <TextBlock x:Name="ErrorTypeText" Text="Grammar" FontSize="11" FontWeight="SemiBold" Foreground="#15C39A" VerticalAlignment="Center" />
          <TextBlock x:Name="CountText" Text="1 of 5" FontSize="10" Foreground="#999999" Margin="10,0,0,0" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal">
          <Button x:Name="PreviousButton" Content="←" Click="PreviousButton_Click" Width="28" Height="28" Background="#F7F7F7" BorderThickness="0" FontSize="14" Foreground="#666666" Cursor="Hand" Margin="0,0,6,0" ToolTip="Previous suggestion">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  CornerRadius="6" Padding="0"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#EEEEEE"/></Trigger></Style.Triggers>
              </Style>
            </Button.Style>
          </Button>

          <Button x:Name="NextButton" Content="→" Click="NextButton_Click" Width="28" Height="28" Background="#F7F7F7" BorderThickness="0" FontSize="14" Foreground="#666666" Cursor="Hand" ToolTip="Next suggestion">
            <Button.Style>
              <Style TargetType="Button">
                <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  CornerRadius="6" Padding="0"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#EEEEEE"/></Trigger></Style.Triggers>
              </Style>
            </Button.Style>
          </Button>
        </StackPanel>
      </Grid>

      <!-- Content with strikethrough and suggestion -->
      <StackPanel Grid.Row="1" Margin="0,0,0,14">
        <TextBlock x:Name="OriginalText" Text="الخطأ" FontSize="13" Foreground="#AAAAAA" TextDecorations="Strikethrough" Margin="0,0,0,6" TextTrimming="CharacterEllipsis" FlowDirection="RightToLeft" />
        <TextBlock x:Name="SuggestionText" Text="الصواب" FontSize="16" FontWeight="SemiBold" Foreground="#2C2C2C" TextWrapping="Wrap" FlowDirection="RightToLeft" />
      </StackPanel>

      <!-- Action buttons -->
      <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="8" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" x:Name="DismissButton" Content="Dismiss" Click="DismissButton_Click" Height="36" Background="Transparent" Foreground="#666666" BorderBrush="#DDDDDD" BorderThickness="1" FontSize="13" FontWeight="Medium" Cursor="Hand" ToolTip="Ignore this suggestion and remove from list">
          <Button.Style>
            <Style TargetType="Button">
              <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="0"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F5F5F5"/><Setter Property=" BorderBrush" Value="#CCCCCC"/></Trigger></Style.Triggers>
            </Style>
          </Button.Style>
        </Button>

        <Button Grid.Column="2" x:Name="AcceptButton" Content="Apply" Click="AcceptButton_Click" Height="36" Background="#15C39A" Foreground="White" BorderThickness="0" FontSize="13" FontWeight="SemiBold" Cursor="Hand" ToolTip="Apply this correction to your document">
          <Button.Style>
            <Style TargetType="Button">
              <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  CornerRadius="8" Padding="0"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#12AB87"/></Trigger></Style.Triggers>
            </Style>
          </Button.Style>
        </Button>
      </Grid>
    </Grid>
  </Border>
</Window>