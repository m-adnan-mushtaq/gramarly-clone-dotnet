<Window x:Class="WordOverlayProofreader.Overlay.SuggestionWidget" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Grammar Suggestions" Height="240" Width="350" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">

  <Border Background="White" BorderBrush="#2196F3" BorderThickness="2" CornerRadius="10" Padding="15">
    <Border.Effect>
      <DropShadowEffect BlurRadius="20" ShadowDepth="3" Opacity="0.4" />
    </Border.Effect>

    <StackPanel>
      <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
        <TextBlock x:Name="ErrorTypeText" Text="Spelling Error" FontWeight="Bold" FontSize="14" Foreground="Red" />
        <TextBlock x:Name="CountText" Text="1 of 5" FontSize="11" Foreground="#666666" Margin="10,2,0,0" VerticalAlignment="Center" />
      </StackPanel>

      <TextBlock Text="Original:" FontSize="11" Foreground="#666666" Margin="0,0,0,3" />
      <Border Background="#FFEBEE" Padding="8" CornerRadius="5" Margin="0,0,0,10">
        <TextBlock x:Name="OriginalText" Text="misteake" FontSize="13" Foreground="#C62828" TextWrapping="Wrap" />
      </Border>

      <TextBlock Text="Suggestion:" FontSize="11" Foreground="#666666" Margin="0,0,0,3" />
      <Border Background="#E8F5E9" Padding="8" CornerRadius="5" Margin="0,0,0,15">
        <TextBlock x:Name="SuggestionText" Text="mistake" FontSize="13" Foreground="#2E7D32" TextWrapping="Wrap" />
      </Border>

      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
        <Button x:Name="PreviousButton" Content="◀ Previous" Click="PreviousButton_Click" Padding="10,5" Margin="0,0,5,0" />
        <Button x:Name="NextButton" Content="Next ▶" Click="NextButton_Click" Padding="10,5" Margin="5,0,0,0" />
      </StackPanel>

      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <Button x:Name="AcceptButton" Content="✓ Accept" Click="AcceptButton_Click" Padding="20,8" Margin="0,0,10,0" Background="#4CAF50" Foreground="White" FontWeight="SemiBold" FontSize="13" />
        <Button x:Name="DismissButton" Content="✗ Dismiss" Click="DismissButton_Click" Padding="20,8" Background="#F44336" Foreground="White" FontWeight="SemiBold" FontSize="13" />
      </StackPanel>
    </StackPanel>
  </Border>
</Window>