<Window x:Class="WordOverlayProofreader.Overlay.OverlayWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:WordOverlayProofreader.Overlay" mc:Ignorable="d" Title="WordOverlayProofreader" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" Loaded="Window_Loaded" WindowState="Maximized">

  <Grid>
    <Canvas x:Name="OverlayCanvas">
      <!-- Squiggles will be drawn here -->
    </Canvas>

    <!-- Compact Grammarly-style tooltip -->
    <Popup x:Name="SuggestionPopup" Placement="Bottom" StaysOpen="True" AllowsTransparency="True" VerticalOffset="8">
      <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Padding="0" MinWidth="220" MaxWidth="320">
        <Border.Effect>
          <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.2" Color="#000000" />
        </Border.Effect>
        <StackPanel>
          <!-- Header with type indicator -->
          <Grid Margin="14,12,14,8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Ellipse Grid.Column="0" Width="6" Height="6" x:Name="TooltipTypeDot" Fill="#15C39A" Margin="0,0,8,0" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" x:Name="ErrorTypeText" FontWeight="SemiBold" FontSize="10" Foreground="#15C39A" VerticalAlignment="Center" Text="Grammar" />
            <Button Grid.Column="2" Content="✕" Click="CloseTooltip_Click" Background="Transparent" BorderThickness="0" Foreground="#CCCCCC" Cursor="Hand" Padding="4" FontSize="11" FontWeight="Normal" ToolTip="Close">
              <Button.Style>
                <Style TargetType="Button">
                  <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="3"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Foreground" Value="#666666"/><Setter Property="Background" Value="#F5F5F5"/></Trigger></Style.Triggers>
                </Style>
              </Button.Style>
            </Button>
          </Grid>

          <!-- Suggestion display (clickable) -->
          <ItemsControl x:Name="SuggestionsList" Margin="0">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Click="ApplySuggestion_Click" Margin="0" Padding="14,10" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Click to apply this suggestion">
                  <StackPanel>
                    <TextBlock Text="{Binding OriginalText}" FontSize="12" Foreground="#AAAAAA" TextDecorations="Strikethrough" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding suggestion}" FontSize="14" FontWeight="SemiBold" Foreground="#2C2C2C" TextWrapping="Wrap" />
                  </StackPanel>
                  <Button.Style>
                    <Style TargetType="Button">
                      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  Padding="{TemplateBinding Padding}"  BorderThickness="0,1,0,0" BorderBrush="#F5F5F5"><ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F8F8F8"/></Trigger></Style.Triggers>
                    </Style>
                  </Button.Style>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>

          <!-- Action buttons -->
          <Grid Margin="14,10,14,12">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="8" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Dismiss" Click="Ignore_Click" Height="32" Background="Transparent" Foreground="#666666" BorderBrush="#DDDDDD" BorderThickness="1" FontSize="12" FontWeight="Medium" Cursor="Hand" ToolTip="Ignore this suggestion">
              <Button.Style>
                <Style TargetType="Button">
                  <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#F5F5F5"/><Setter Property=" BorderBrush" Value="#CCCCCC"/></Trigger></Style.Triggers>
                </Style>
              </Button.Style>
            </Button>

            <Button Grid.Column="2" x:Name="ApplyFromTooltip" Content="Apply" Click="ApplySuggestion_Click" Height="32" Background="#15C39A" Foreground="White" BorderThickness="0" FontSize="12" FontWeight="SemiBold" Cursor="Hand" ToolTip="Apply this correction">
              <Button.Style>
                <Style TargetType="Button">
                  <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="6"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Setter.Value></Setter><Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#12AB87"/></Trigger></Style.Triggers>
                </Style>
              </Button.Style>
            </Button>
          </Grid>
        </StackPanel>
      </Border>
    </Popup>
  </Grid>
</Window>