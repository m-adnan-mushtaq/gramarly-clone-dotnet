<Window x:Class="WordOverlayProofreader.Overlay.OverlayWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:WordOverlayProofreader.Overlay" mc:Ignorable="d" Title="WordOverlayProofreader" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" Loaded="Window_Loaded" WindowState="Maximized">

  <Grid>
    <Canvas x:Name="OverlayCanvas">
      <!-- Squiggles will be drawn here -->
    </Canvas>

    <!-- Popover for suggestions -->
    <Popup x:Name="SuggestionPopup" Placement="Bottom" StaysOpen="True" AllowsTransparency="True" VerticalOffset="5">
      <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="0">
        <Border.Effect>
          <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.15" Color="Black" />
        </Border.Effect>
        <StackPanel MinWidth="180" MaxWidth="300">
            <!-- Header with Type and Close -->
            <Grid Margin="12,10,12,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="ErrorTypeText" FontWeight="SemiBold" FontSize="11" Foreground="#757575" VerticalAlignment="Center" Text="SPELLING" />
                <Button Grid.Column="1" Content="✕" Click="CloseTooltip_Click" Background="Transparent" BorderThickness="0" Foreground="#BDBDBD" Cursor="Hand" Padding="5,0" FontSize="10" FontWeight="Bold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#555555"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Suggestion List -->
            <ItemsControl x:Name="SuggestionsList" Margin="0,0,0,5">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Click="ApplySuggestion_Click" Margin="0" Padding="12,8" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <TextBlock Text="{Binding suggestion}" FontSize="14" FontWeight="Medium" Foreground="#212121"/>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Footer Actions -->
            <Border BorderBrush="#F0F0F0" BorderThickness="0,1,0,0" Padding="0">
                <Button Content="Ignore" Click="Ignore_Click" Background="Transparent" BorderThickness="0" Foreground="#9E9E9E" Padding="12,8" HorizontalContentAlignment="Left" Cursor="Hand" FontSize="11">
                     <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FAFAFA"/>
                                    <Setter Property="Foreground" Value="#757575"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>
        </StackPanel>
      </Border>
    </Popup>
  </Grid>
</Window>