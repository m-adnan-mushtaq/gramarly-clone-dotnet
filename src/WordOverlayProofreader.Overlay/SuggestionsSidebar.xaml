<Window x:Class="WordOverlayProofreader.Overlay.SuggestionsSidebar" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Grammar Suggestions" Width="380" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">

  <Border Background="#F5F5F5" BorderBrush="#2196F3" BorderThickness="3,0,0,0" Padding="0">
    <Border.Effect>
      <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.3" Direction="180" />
    </Border.Effect>

    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <!-- Header -->
      <Border Grid.Row="0" Background="#2196F3" Padding="15,12">
        <StackPanel>
          <TextBlock x:Name="HeaderText" Text="Grammar Suggestions (0)" FontWeight="Bold" FontSize="16" Foreground="White" />
          <TextBlock Text="Click Accept to fix or Dismiss to ignore" FontSize="11" Foreground="#E3F2FD" Margin="0,3,0,0" />
        </StackPanel>
      </Border>

      <!-- Scrollable suggestions list -->
      <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="15,15,15,15">
        <StackPanel x:Name="SuggestionsPanel">
          <!-- Suggestion cards will be added here dynamically -->
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Border>
</Window>